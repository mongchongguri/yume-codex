interface:
  display_name: "Build Progress"
  short_description: "Run long builds with progress reports."
  default_prompt: "Use $build-progress to run a build command and report progress while it runs."
policy:
  allow_implicit_invocation: false
