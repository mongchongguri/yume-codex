interface:
  display_name: "Context Reader"
  short_description: "Read latest saved summary, worklog, or handoff context."
  default_prompt: "Use $context-reader to read the latest handoff and continue the current work."
policy:
  allow_implicit_invocation: false
