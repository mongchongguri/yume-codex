interface:
  display_name: "Icon Image"
  short_description: "Generate PNG icon assets and apply them."
  default_prompt: "Use $icon-image to create an original PNG icon asset and apply it to the project."
policy:
  allow_implicit_invocation: false
